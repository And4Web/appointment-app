testing git on new device.

// Heroku build configuration
// if(process.env.NODE_ENV === "PRODUCTION"){
//   app.use('/' . express.static("client/build"));
//   app.get("*", (req, res)=>{
//     res.sendFile(path.resolve(__dirname, "client/build/index.html"));
//   });
// }
